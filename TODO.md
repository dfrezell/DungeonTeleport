# TODO

- Add tooltip refresh ticker for raid buttons to keep cooldowns up to date on hover.
- Review raid list button sizing/anchoring for UI scale/locales and adjust if needed.
- Clear the secure `spell` attribute when unknown to prevent any click errors.
